{% extends 'base.html.twig' %}

{% block title %}Tickets{% endblock %}

{% block body %}
    <h1>Tickets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ ticket.content }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ ticket.title }}</td>
            </tr>
           
            <tr>
                <th>Dates</th>
                <td>{{ ticket.dates ? ticket.dates|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Client_name</th>
                <td>{{ ticket.clientName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tickets_index') }}">back to list</a>

    <a href="{{ path('tickets_edit', {'id': ticket.id}) }}">edit</a>
    <a href="{{ path('tickets_edit', {'id': ticket.id}) }}">edit</a>

    {% if is_granted('ROLE_ADMIN') %} {{ include('tickets/_delete_form.html.twig') }} {% endif %}
    

 <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Content</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for message in messages %}
            <tr>
                <td>{{ message.id }}</td>
                <td>{{ message.content }}</td>
                <td>
                    <a href="{{ path('message_show', {'id': message.id}) }}">show</a>
                    <a href="{{ path('message_edit', {'id': message.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
               
        {% endfor %}
        </tbody>
    </table>
    
        <div>
        {{ include('./message/_form.html.twig') }}  </div>



{% endblock %}
